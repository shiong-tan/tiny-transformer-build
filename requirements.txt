# Tiny Transformer Course - Python Dependencies
# Minimal set of packages required for the course
# Tested with Python 3.11 and 3.12

# Core ML Framework
# Install PyTorch separately based on your platform:
# macOS (Apple Silicon - MPS):
#   pip install torch torchvision torchaudio
# macOS (Intel):
#   pip install torch torchvision torchaudio
# Linux (CUDA 12.1):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# Linux (CPU only):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
#
# Official guide: https://pytorch.org/get-started/locally/

torch>=2.0.0
numpy>=1.24.0

# Jupyter for interactive notebooks (optional but recommended)
jupyter>=1.0.0
notebook>=7.0.0
ipykernel>=6.25.0

# Visualization
matplotlib>=3.7.0

# Progress bars
tqdm>=4.66.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Data handling
pandas>=2.0.0  # For logging and CSV-based run tracking

# Code quality (optional - for development)
black>=23.0.0
flake8>=6.0.0

# Notebook validation
nbclient>=0.8.0
nbformat>=5.9.0

# YAML configuration support
pyyaml>=6.0.0

# Type hints
typing-extensions>=4.8.0
